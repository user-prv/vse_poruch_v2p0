# План міграції `vseporuch-old` на стек Golang + React

## 0) Підготовка репозиторію
- [x] Перенести/підключити старий PHP-код у папку `vseporuch-old/` (якщо ще не додано).
- [ ] Зафіксувати цільову структуру монорепозиторію:
  - [x] `backend/` — API на Go (Gin + GORM + Logrus).
  - [x] `frontend/` — клієнт на React.
  - [x] `vseporuch-old/` — legacy PHP як референс на перехідний період.
- [x] Додати `README.md` з коротким описом архітектури та сценарію запуску кожної частини.

## 1) Аудит legacy-системи (PHP)
- [x] Проінвентаризувати модулі, ендпоінти, cron/jobs, інтеграції та ролі користувачів.
- [x] Скласти карту бізнес-функцій: "що є", "що критично", "що можна відкласти".
- [x] Зафіксувати поточну схему БД, зв’язки, індекси, обмеження та міграції.
- [x] Визначити технічний борг і ризики під час міграції (безпека, продуктивність, сумісність).

## 2) Проєктування цільової архітектури
- [x] Спроєктувати API-контракти (REST/JSON) для всіх критичних сценаріїв.
- [x] Визначити шари `backend/`: `handler -> service -> repository -> db`.
- [x] Визначити підхід до логування (Logrus): формат, рівні, correlation/request id.
- [x] Визначити стратегію доступу до віддаленої БД (env-конфіг, SSL/TLS, пул з’єднань, таймаути).
- [x] Затвердити модель стану на фронтенді (React state management, API client, error handling).

## 3) Ініціалізація `backend/` (Golang)
- [x] Створити Go-проєкт у `backend/` з базовими залежностями: `gin`, `gorm`, `logrus`.
- [x] Реалізувати конфігурацію через env (`.env.example`): `DB_HOST`, `DB_PORT`, `DB_NAME`, `DB_USER`, `DB_PASSWORD`, `DB_SSLMODE`.
- [x] Налаштувати підключення до віддаленої БД через GORM + healthcheck ендпоінт.
- [x] Додати middleware у Gin: recovery, request logging, CORS, request id.
- [x] Впровадити базову структуру модулів (auth/users/... відповідно до legacy-аудиту).

## 4) Ініціалізація `frontend/` (React)
- [x] Створити React-проєкт у `frontend/` (Vite або інший узгоджений інструмент).
- [x] Налаштувати структуру: `pages/`, `components/`, `services/api/`, `hooks/`.
- [x] Реалізувати базовий API-клієнт (base URL, обробка помилок, retry/pending-state).
- [x] Підготувати базові layout/route guards для ролей та авторизації.
- [x] Додати змінні оточення (`.env.example`) для URL бекенду.

## 5) Поетапна міграція функціоналу
- [x] Пріоритезувати фічі у хвилі міграції (Wave 1/2/3).
- [ ] Для кожної фічі виконувати цикл:
  - [x] Описати API-контракт.
  - [x] Реалізувати backend endpoint + service + repository.
  - [x] Реалізувати відповідний UI у React.
  - [ ] Провести функціональну перевірку проти поведінки `vseporuch-old`.
- [x] Вести migration checklist відповідності "legacy -> new".

## 6) Дані та сумісність
- [x] Підготувати SQL/GORM-міграції (якщо потрібна еволюція схеми).
- [x] Реалізувати скрипти міграції/синхронізації довідників та критичних даних.
- [x] Перевірити зворотну сумісність форматів даних для фронтенду та інтеграцій.
- [x] Описати rollback-процедури у випадку невдалого релізу.

## 7) Якість, безпека, спостережуваність
- [x] Додати базові unit/integration тести для ключових backend-сервісів.
- [x] Додати frontend-тести для критичних користувацьких сценаріїв.
- [x] Перевірити authN/authZ, валідацію вхідних даних, захист секретів.
- [x] Налаштувати централізовані логи (Logrus), метрики та алерти.

## 8) Реліз і decommission legacy
- [ ] Запустити staging з повним smoke/regression тестом.
- [ ] Провести канарний або поетапний прод-реліз.
- [ ] Моніторити помилки/латентність і оперативно закривати інциденти.
- [ ] Після стабілізації — формально відключити `vseporuch-old` і оновити документацію.

## Definition of Done
- [ ] Усі критичні legacy-сценарії працюють у новому стеку (Go + React).
- [x] `backend/` і `frontend/` мають інструкції запуску та деплою.
- [ ] Віддалене підключення до БД стабільне, логування через Logrus активне.
- [ ] Legacy PHP не використовується у production-трафіку.
